<?php
    require_once('db.php');
	session_start();
    // print_r(get_feedback_reject_task(1));
    // $user = $_SESSION['user'];
    // echo $user;
    // $role = get_info_employee_byuser($_SESSION['user'])['role'];
    // echo $role;
    
?>